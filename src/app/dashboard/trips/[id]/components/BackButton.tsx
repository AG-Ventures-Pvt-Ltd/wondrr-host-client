import BackButton from '@/common/ui/BackButton';

const TripBackButton: React.FC = () => {
    return <BackButton label="Back to All Destinations" to="/dashboard/trips" />;
};

export default TripBackButton;
